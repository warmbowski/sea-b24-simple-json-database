var chai = require('chai');
var chaihttp = require('chai-http');
chai.use(chaihttp);

require('../lib/server.js');

var expect = chai.expect;

describe('api create and read verification', function() {
  
  it('should be able to create a record with json content', function() {
    
  });

  it('should be able to get json content from a record', function() {
    
  });
});